import { DateTimeUfficialPipe } from './time-converter.pipe';

describe('DateTimeUfficialPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeUfficialPipe();
    expect(pipe).toBeTruthy();
  });
});
